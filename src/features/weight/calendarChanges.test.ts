import{describe,expect,it}from"vitest";import{calendarChanges,dailyWeightAverages}from"./calendarChanges";import type{Reading}from"./logic";const r=(id:string,date:string,weight:number,period:"morning"|"evening"="morning"):Reading=>({id,measuredAt:`${date}T12:00:00`,period,weight,unit:"lb"});
describe("calendar bodyweight changes",()=>{it("averages multiple readings once per day and keeps periods independent",()=>{const rows=[r("a","2024-01-01",100),r("b","2024-01-01",102,"evening")];expect(dailyWeightAverages(rows,"both")[0].value).toBe(101);expect(dailyWeightAverages(rows,"morning")[0].value).toBe(100)});it("uses Monday-Sunday weeks with changes and missing-period gaps",()=>{const rows=[r("a","2024-01-01",100),r("b","2024-01-08",102),r("c","2024-01-22",110)],points=calendarChanges(rows,"weekly","both",new Date(2024,0,22,12));expect(points[0].label).toBe("2024-01-01 – 2024-01-07");expect(points[1]).toMatchObject({change:2,percentChange:2});expect(points[2].change).toBeNull();expect(points[2].partial).toBe(true)});it("handles month/year transitions, decimals, and zero safely",()=>{const points=calendarChanges([r("a","2023-12-31",0),r("b","2024-01-01",100.25)],"monthly","both",new Date(2024,0,15));expect(points.map(point=>point.key)).toEqual(["2023-12","2024-01"]);expect(points[1].change).toBe(100.25);expect(points[1].percentChange).toBeNull();expect(points[0].change).toBeNull()})});
