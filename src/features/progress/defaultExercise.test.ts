import{describe,expect,it}from"vitest";import{defaultStrengthExercise}from"./defaultExercise";import repository from"./repository.ts?raw";import progress from"./ProgressFeature.tsx?raw";
const row=(exerciseId:string,exerciseName:string,date:string)=>({exerciseId,exerciseName,date});
describe("default Strength Progress exercise",()=>{it("counts every actual set including multi-set entries",()=>expect(defaultStrengthExercise([row("bench","Bench Press","2024-01-01"),row("bench","Bench Press","2024-01-01"),row("squat","Squat","2025-01-01")])?.id).toBe("bench"));it("breaks count ties by latest performance date",()=>expect(defaultStrengthExercise([row("a","Alpha","2024-01-01"),row("b","Beta","2024-02-01")])?.id).toBe("b"));it("breaks complete ties by normalized exercise name",()=>expect(defaultStrengthExercise([row("z","  chin   Up ","2024-01-01"),row("a","Bench Press","2024-01-01")])?.id).toBe("a"));it("keeps weighted and unweighted exercise IDs separate",()=>expect(defaultStrengthExercise([row("pull","Pull Up","2024-01-01"),row("weighted","Weighted Pull Up","2024-01-01"),row("weighted","Weighted Pull Up","2024-01-02")])?.id).toBe("weighted"));it("returns no selection for an account without sets",()=>expect(defaultStrengthExercise([])).toBeUndefined());it("uses one account-scoped, unpaginated actual-set query",()=>{expect(repository).toContain('.from("strength_sets")');expect(repository).toContain('.eq("user_id",userId)');expect(repository).not.toContain(".range(");expect(repository).toContain("strength_set_workout_owned_fk")});it("keeps manual selection in mounted state while controls change",()=>{expect(progress).toContain("selected ?? automatic?.id");expect(progress).toContain("setSelected(value)");expect(progress).not.toContain("setSelected(automatic");expect(progress).toContain("No {exerciseName} entries in this date range")})});
