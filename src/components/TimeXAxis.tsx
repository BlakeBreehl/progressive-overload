import{XAxis,useChartWidth}from"recharts";
import{compactTimeLabel,selectFormattedTimeTicks,timeTickLimit,type TimeAxisMode}from"../lib/timeAxis";
export function TimeXAxis({dates,dataKey="date",mode="auto",values}:{dates:string[];values?:string[];dataKey?:string;mode?:TimeAxisMode}){const width=useChartWidth()??320;const ticks=selectFormattedTimeTicks(dates,timeTickLimit(width),mode);return <XAxis dataKey={dataKey} ticks={values?ticks.map(date=>values[dates.indexOf(date)]):ticks} tickFormatter={value=>compactTimeLabel(values?dates[values.indexOf(String(value))]??dates[0]:String(value),dates,mode)} minTickGap={18} tick={{fontSize:11,fill:"#777"}} interval="preserveStartEnd"/>}
